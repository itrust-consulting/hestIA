entrypoint: Chat1
exitpoints: [Chat1]

include:
  - ../base/chat.yaml

nodes:
  - use: chat.yaml
    with:
      id: Chat1
      inputs:
        history: ${history} 
        last_user_message: ${last_user_message}
        model: ${model}
        options: ${model_kwargs}
      outputs:
        response: response  